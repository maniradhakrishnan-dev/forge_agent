# /tests — Unit and Integration Test Suite

This directory contains test modules for ForgeAgent components, executed via `uv run pytest`.

## Test Modules

- `test_basic_loop.py`: Tests CadQuery execution, single-part OpenCascade verification, and the basic P0 generate-verify-repair loop.
